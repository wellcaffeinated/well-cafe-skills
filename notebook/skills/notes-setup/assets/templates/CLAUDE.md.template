# {{Vault Name}}

> Agent orientation for this vault. Always loaded. Keep it short — only what most interactions need. Detail lives in `_Meta/` and is linked from the index below.

## Purpose

{{One or two lines: what this vault is for, in the user's words.}}

## Folder map

| Folder | What lives here |
|--------|-----------------|
| `{{00 Daily}}` | {{daily capture}} |
| `{{_Inbox}}` | {{unfiled captures awaiting processing}} |
| `{{...}}` | {{...}} |

## Inviolable rules

- Never `overwrite` a note unless the user explicitly asks.
- {{Agent drafts land in {{staging}} with `status: draft` until reviewed.}}
- {{...}}

## Note types

{{One line each, or link to a types/ doc for those with real machinery.}}

- `{{bookmark}}` → `_Meta/types/{{bookmarks}}.md`
- `{{project}}` → `_Meta/types/{{projects}}.md`

## Index

- Conventions: {{`_Meta/conventions/where-things-go.md`}}, {{`_Meta/conventions/properties.md`}}
- Types: {{see above}}
- Workflows: {{`_Meta/workflows/...`}}
- Dashboards: {{`_Dashboard/...`}}
